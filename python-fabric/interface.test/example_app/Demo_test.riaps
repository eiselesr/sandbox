app Demo
{
    // The fault injection app is a simple app that injects faults into the system

    library app;
    library test_suite;  // Required for monkey patching

    device DemoDevice_test()
    {
    	timer tick 1000;
    }
		
	real-time actor DemoDeviceActor()
	{
		{ 
			demo_device  : DemoDevice_test();  // Required for monkey patching
		}
	}
}
